<html>
    <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>SITE HAS BEEN SEIZED!</title>
    <meta content='width=device-width, initial-scale=1' name='viewport'/>
    <meta content='blogger' name='generator'/>
    <meta content="description" name="hacked by UNDERSOLFIDBYTE">
    <link href="https://fonts.googleapis.com/css2?family=Genos&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    </head>
    <style>
    body  {
      Background: #000;
      Color: white;
      text-align: center;
      font-size: 16pt;
      font-family: 'Genos', sans-serif;
      letter-spacing: 2px;
      line-height: 25px;
      margin: 0;
      padding: 0;
      cursor: pointer;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }
    a   {
      text-decoration: none;
      color: white;
    }
    a:hover {
      text-decoration: none;
      color: maroon;
    }
    img {
      border-radius: 50%;
      padding: 15px;
      box-shadow: 1px 2px #F56057;
      width: 150px;
      height: 150px;
      animation: rotateYAnimation 3s ease-in-out infinite alternate;
      transform-style: preserve-3d;
    }
    d7net {
      font-size: 16pt;
      color: #F87169;
    }
    .click-instruction {
      margin-top: 30px;
      font-size: 14pt;
      color: #888;
      padding: 10px 20px;
      border: 1px dashed #F56057;
      border-radius: 10px;
      max-width: 80%;
      animation: pulse 2s infinite;
    }
    .audio-controls {
      margin-top: 20px;
      display: flex;
      justify-content: center;
      gap: 15px;
    }
    .audio-btn {
      background-color: #333;
      border: 1px solid #F56057;
      color: white;
      padding: 8px 15px;
      border-radius: 5px;
      cursor: pointer;
      font-family: 'Genos', sans-serif;
      font-size: 14pt;
      transition: all 0.3s;
    }
    .audio-btn:hover {
      background-color: #F56057;
      color: black;
    }
    @keyframes rotateYAnimation {
      0% {
        transform: rotateY(0deg);
        box-shadow: 1px 2px #F56057;
      }
      25% {
        transform: rotateY(90deg);
        box-shadow: 5px 5px 10px #F56057;
      }
      50% {
        transform: rotateY(180deg);
        box-shadow: 1px 2px #F56057;
      }
      75% {
        transform: rotateY(270deg);
        box-shadow: 5px 5px 10px #F56057;
      }
      100% {
        transform: rotateY(360deg);
        box-shadow: 1px 2px #F56057;
      }
    }
    @keyframes pulse {
      0% {
        opacity: 0.7;
      }
      50% {
        opacity: 1;
      }
      100% {
        opacity: 0.7;
      }
    }
    </style> 
    <body>
    <br><br>
    <img src='https://f.top4top.io/p_3662lf0mt1.png' alt="Logo"/>
    <br><br>
    HACKED BY<d7net>UNDERSOLFIDBYTE</d7net><br><br>
    We stand to eradicate the sites of India, Israel, and Nato allies - Gaza's revenge for you<br><d7net>- WE UNDERSOLFIDBYTE TEAM -</d7net>
    
    <div class="click-instruction">
      WE ARE SUPPORTERS OF THE STRUGGLE OF RUSSIA AND PALESTINE!
    </div>
    
    <div class="audio-controls">
      <button class="audio-btn" id="playBtn">PLAY</button>
      <button class="audio-btn" id="pauseBtn">PAUSE</button>
      <button class="audio-btn" id="stopBtn">STOP</button>
    </div>
    
    <audio id="bgMusic" loop>
      <source src="https://h.top4top.io/m_3663ns36a1.mp3" type="audio/mpeg">
      Browser Anda tidak mendukung elemen audio.
    </audio>

    <script>
      // Elemen audio
      const bgMusic = document.getElementById('bgMusic');
      const playBtn = document.getElementById('playBtn');
      const pauseBtn = document.getElementById('pauseBtn');
      const stopBtn = document.getElementById('stopBtn');
      
      // Fungsi untuk memutar musik
      function playMusic() {
        bgMusic.play().catch(e => {
          console.log("Autoplay diblokir, silakan klik tombol putar: ", e);
        });
      }
      
      // Fungsi untuk menghentikan musik
      function stopMusic() {
        bgMusic.pause();
        bgMusic.currentTime = 0;
      }
      
      // Putar musik saat halaman diklik di mana saja
      document.body.addEventListener('click', function(event) {
        // Jika yang diklik adalah tombol kontrol, biarkan tombol tersebut menangani
        if (event.target.classList.contains('audio-btn')) {
          return;
        }
        
        // Jika musik sedang diputar, jeda. Jika tidak, putar.
        if (bgMusic.paused) {
          playMusic();
        } else {
          bgMusic.pause();
        }
      });
      
      // Kontrol tombol
      playBtn.addEventListener('click', function() {
        playMusic();
      });
      
      pauseBtn.addEventListener('click', function() {
        bgMusic.pause();
      });
      
      stopBtn.addEventListener('click', function() {
        stopMusic();
      });
      
      // Tambahkan event listener untuk memungkinkan autoplay setelah interaksi pengguna
      document.addEventListener('DOMContentLoaded', function() {
        // Coba putar musik saat halaman dimuat (mungkin diblokir browser)
        playMusic();
      });
    </script>
    </body>
</html>
